{
  "name": "wallet-pass",
  "version": "1.0.0",
  "description": "Google Wallet Pass Generator",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "prebuild": "npm run clean",
    "prepare": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "google",
    "wallet",
    "pass",
    "generic",
    "jwt"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "jsonwebtoken": "^9.0.0",
    "fs": "0.0.1-security"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^9.0.0",
    "@types/node": "^18.0.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.9.0"
  }
}
